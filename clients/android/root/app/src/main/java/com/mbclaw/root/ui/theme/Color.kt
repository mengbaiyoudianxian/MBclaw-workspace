package com.mbclaw.root.ui.theme

import androidx.compose.ui.graphics.Color

// ────────────────────────────────────────────────────────
// MBclaw v3 调色 — 仿 MiClaw / 小米 HyperOS 风格
// 主色：小米官方橙   FF6900
// 辅色：小米晴空蓝   3B82F6
// 暗色背景源自 HyperOS Dark 「云墨灰」 #1A1A1F
// ────────────────────────────────────────────────────────

// 品牌色
val MBclawOrange = Color(0xFFFF6900)   // 主色 — Xiaomi 橙
val MBclawBlue   = Color(0xFF3B82F6)   // 辅色 — 晴空蓝
val MBclawGreen  = Color(0xFF34C759)   // iOS 绿(状态成功)
val MBclawRed    = Color(0xFFFF3B30)   // iOS 红(错误)
val MBclawPurple = Color(0xFFAF52DE)

// 暗色 (HyperOS Dark 风格 — 偏蓝紫的深灰)
val DarkBackground     = Color(0xFF0F0F12)   // 全局底
val DarkSurface        = Color(0xFF1A1A1F)   // 卡片
val DarkSurfaceVariant = Color(0xFF24242B)   // 二级卡片
val DarkBorder         = Color(0xFF2E2E36)
val DarkText           = Color(0xFFF5F5F7)   // 标题
val DarkTextMuted      = Color(0xFF98989E)   // 副标题

// 浅色 (HyperOS Light — 米白卡片 + 浅灰底)
val LightBackground = Color(0xFFF2F2F7)
val LightSurface    = Color(0xFFFFFFFF)
val LightBorder     = Color(0xFFE5E5EA)
val LightText       = Color(0xFF1C1C1E)
val LightTextMuted  = Color(0xFF8E8E93)
